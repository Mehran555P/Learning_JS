// References types are Objects, Arrays and Functions.

// Object

let person = {
    name: "Mehran",
    age: 18
};

// Dot Notation
person.name = 'something else';

// Bracket Notation
person['name'] = 'something else';

console.log(person.name)